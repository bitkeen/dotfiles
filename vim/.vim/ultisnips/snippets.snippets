# Taken from https://github.com/honza/vim-snippets/blob/master/UltiSnips/snippets.snippets
# and modified.
#
# We use a little hack so that the snippet is expanded
# and parsed correctly.
snippet sn "Ultisnips snippet definition" b
`!p snip.rv = "snippet"` ${1:trigger}
${0:${VISUAL}}
`!p snip.rv = "endsnippet"`
endsnippet

snippet snd "Ultisnips snippet definition with description" b
`!p snip.rv = "snippet"` ${1:trigger} "${2:Description}"
${0:${VISUAL}}
`!p snip.rv = "endsnippet"`
endsnippet
