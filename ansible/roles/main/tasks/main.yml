---
- name: create directories in user_home
  file:
    path: "{{ user_home }}/{{ item.path }}"
    state: directory
    owner: "{{ user_name }}"
    group: "{{ user_group }}"
  loop:
    - { path: .local/bin }
    - { path: .vim }
    - { path: .vim/ultisnips }
