#!/bin/sh
# Open a book from the ~/books directory.
#
# Dependencies:
# - rifle
# - dmenu

xdg-open "$( \
    find -L ~/books -type f -name '*.pdf' -o -name '*.epub' -o -name '*.djvu' |
    dmenu-vertical
)"
