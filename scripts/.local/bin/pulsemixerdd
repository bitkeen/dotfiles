#!/bin/sh
# Open a terminal window with pulsemixer if it doesn't already exist,
# kill it otherwise.

pgrep -f "$TERMINAL -n pulsemixerdd" | xargs kill 2>/dev/null ||
    "$TERMINAL" -n 'pulsemixerdd' -e zsh -i -c 'pulsemixer'
